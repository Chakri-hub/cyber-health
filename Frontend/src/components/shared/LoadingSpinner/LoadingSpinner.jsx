import React from 'react';
import CustomLoader from '../Loading Animation/CustomLoader';
import './LoadingSpinner.css';

function LoadingSpinner() {
  return <CustomLoader />;
}

export default LoadingSpinner; 